cmake==3.25.2
ninja==1.11.1
fairseq==0.12.2
# Upgrade hydra-core / omegaconf to a modern pair; avoid legacy omegaconf 2.0.x invalid metadata.
# fairseq 0.12.2 tolerates newer hydra-core (used mainly for config utilities).
hydra-core==1.3.2
omegaconf==2.3.0
setuptools==59.5.0
wheel==0.38.4
pyhesaff==2.1.1
